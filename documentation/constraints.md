# Constraints

## Imports from and exports to a Google Sheet

This constraints stems from the current workflow of draft management.

## Runs in a Windows environment

This is not really a hard constraint, but the leagues are typically run on either a VPS or a home server with Windows. So ideally the application can be run on the same server to make it easier to set up for admins.
